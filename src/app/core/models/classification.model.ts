export interface Anatomy {
  id: string;
  name: string;
}

export interface Equipment {
  id: string;
  name: string;
}

export interface TrainingLevel {
  id: string;
  name: string;
}

export interface ContentStatus {
  id: string;
  name: string;
}

export interface RpeScale {
  id: string;
  name: string;
}
